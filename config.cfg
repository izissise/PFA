set r_width 1600
set r_height 900

bind Z forward
bind W forward
bind S back
bind D right
bind A left
bind Q left
bind F1 console
bind F use
bind MWHEELDOWN console
bind F4 console
bind SPACE moveup
bind L_CTRL movedown
bind RIGHTARROW right
bind LEFTARROW left
